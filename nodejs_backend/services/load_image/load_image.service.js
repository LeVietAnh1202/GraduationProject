// Mock service để lấy ảnh từ server
async function getImagesRootByStudentId(studentId) {
    const imageData = ''; // Thực hiện logic để lấy dữ liệu ảnh từ server
    return imageData;
}

module.exports = {
    getImagesRootByStudentId
};
